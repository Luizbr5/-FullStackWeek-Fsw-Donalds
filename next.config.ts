import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["u9a6wmr3as.ufs.sh"], // Add your domain here
  },
};

module.exports = {
  images: {
    domains: ["u9a6wmr3as.ufs.sh"], // Adicione seu domínio aqui
  },
};

export default nextConfig;